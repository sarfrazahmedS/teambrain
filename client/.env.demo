# Loaded by Vite when building/running with `--mode demo`
# (npm run dev:demo / npm run build:demo).
# Turns on the in-memory mock backend so the app runs as a static live demo
# with no server or database. Never used by normal dev/prod builds.
VITE_DEMO=1
